services:
  - type: web
    name: plant-disease-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python webapp/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
